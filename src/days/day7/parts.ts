import { ParseResult } from "./parse";

export const part1 = (rows: ParseResult) => {
  let result = 0;

  return result;
};

export const part2 = (rows: ParseResult) => {
  let result = 0;

  return result;
};
